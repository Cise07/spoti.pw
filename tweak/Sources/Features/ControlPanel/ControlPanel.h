// A Live Activity control panel with tabs for playback options, the queue, devices and a sleep timer.
// For now a mockup on made-up data, to try the layout and the taps on the phone before it drives Spotify.
// The widget is extension/LiveActivity, built into the IPA by scripts/build-extension.sh.
#import <Foundation/Foundation.h>

#define SGKeyControlPanel @"spotifyglass.controlPanel"
